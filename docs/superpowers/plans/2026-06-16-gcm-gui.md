# gcm GUI (double-click) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Khi double-click `gcm.exe`, mở cửa sổ Tkinter để stage file, sinh commit message bằng AI, sửa và commit/push; CLI hiện tại giữ nguyên.

**Architecture:** Một file `gcm.exe` (PyInstaller `--onefile`). Trong `main()` phát hiện double-click (no args + console mới do Explorer tạo) → ẩn console và gọi `gcm_gui.run_gui(core)`, truyền chính module `gcm` làm `core`. GUI (`gcm_gui.py`) KHÔNG import ngược `gcm` (file không có đuôi `.py`) mà dùng `core` được truyền vào. Các hàm lõi mới (`save_config`, `set_repo`, `is_double_clicked`, `stage_files`, `generate_message`, `push_simple`) là hàm thuần, không UI, test bằng pytest; phần Tkinter + wiring + build kiểm thủ công trên Windows.

**Tech Stack:** Python 3 stdlib (subprocess, ctypes, tkinter, importlib), pytest (dev), PyInstaller + WiX (build, Windows).

---

## File Structure

| File | Trách nhiệm |
|------|-------------|
| `gcm` | CLI + toàn bộ logic lõi. Thêm các hàm thuần dùng chung cho GUI và bước phát hiện double-click trong `main()`. |
| `gcm_gui.py` (mới) | Cửa sổ Tkinter (layout 2 cột). Nhận `core` (module gcm) qua tham số, không import gcm. |
| `tests/conftest.py` (mới) | Load file `gcm` (không đuôi `.py`) thành module `gcm` qua importlib; fixture repo git tạm. |
| `tests/test_core.py` (mới) | Test các hàm lõi mới. |
| `packaging/windows/build.ps1` | Thêm `gcm_gui.py` cạnh entry + `--hidden-import gcm_gui` khi build. |

**Quy ước test git tạm:** mọi test chạm git phải `os.chdir` vào repo tạm và set `core.REPO_ROOT`. `run_git` dùng `cwd=REPO_ROOT`, nên fixture set `core.REPO_ROOT = str(tmp_repo)`.

---

## Task 1: Test harness — load `gcm` as a module + tmp git repo fixture

**Files:**
- Create: `tests/conftest.py`
- Create: `tests/test_core.py`

- [ ] **Step 1: Write conftest that imports the extension-less `gcm` file**

Create `tests/conftest.py`:

```python
import importlib.util
import os
import subprocess
import sys
from pathlib import Path

import pytest

REPO_DIR = Path(__file__).resolve().parent.parent
GCM_PATH = REPO_DIR / "gcm"


@pytest.fixture
def core():
    """Load the extension-less `gcm` script as an importable module."""
    spec = importlib.util.spec_from_loader(
        "gcm", importlib.machinery.SourceFileLoader("gcm", str(GCM_PATH))
    )
    module = importlib.util.module_from_spec(spec)
    sys.modules["gcm"] = module
    spec.loader.exec_module(module)
    yield module
    sys.modules.pop("gcm", None)


@pytest.fixture
def git_repo(tmp_path, core, monkeypatch):
    """A fresh git repo as cwd, with core.REPO_ROOT pointed at it."""
    repo = tmp_path / "repo"
    repo.mkdir()
    env = {**os.environ, "GIT_AUTHOR_NAME": "t", "GIT_AUTHOR_EMAIL": "t@t",
           "GIT_COMMITTER_NAME": "t", "GIT_COMMITTER_EMAIL": "t@t"}
    for args in (["init", "-q"], ["commit", "-q", "--allow-empty", "-m", "init"]):
        subprocess.run(["git", *args], cwd=repo, env=env, check=True)
    monkeypatch.chdir(repo)
    monkeypatch.setattr(core, "REPO_ROOT", str(repo))
    return repo
```

- [ ] **Step 2: Write a smoke test that the module loads**

Create `tests/test_core.py`:

```python
def test_module_loads(core):
    assert core.VERSION
    assert callable(core.changed_files)
```

- [ ] **Step 3: Run the smoke test**

Run: `cd /home/devduide/devduide_project/tools/gcm && python -m pytest tests/test_core.py -v`
Expected: PASS (1 passed). If pytest missing: `python -m pip install pytest`.

- [ ] **Step 4: Commit**

```bash
git add tests/conftest.py tests/test_core.py
git commit -m "test: add harness to load gcm module and tmp git repo fixture

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: `save_config(key, value)` — persist a config key

**Files:**
- Modify: `gcm` (add function after `load_config`, around line 92)
- Test: `tests/test_core.py`

- [ ] **Step 1: Write the failing test**

Append to `tests/test_core.py`:

```python
def test_save_config_creates_and_updates(core, tmp_path, monkeypatch):
    cfg = tmp_path / "config"
    monkeypatch.setattr(core, "CONFIG_PATH", str(cfg))

    core.save_config("api_key", "gsk_abc")
    assert "api_key = gsk_abc" in cfg.read_text(encoding="utf-8")

    core.save_config("api_key", "gsk_xyz")  # update in place
    text = cfg.read_text(encoding="utf-8")
    assert "gsk_xyz" in text and "gsk_abc" not in text
    assert text.count("api_key") == 1  # not duplicated


def test_save_config_preserves_other_lines(core, tmp_path, monkeypatch):
    cfg = tmp_path / "config"
    cfg.write_text("# comment\nlang = vi\n", encoding="utf-8")
    monkeypatch.setattr(core, "CONFIG_PATH", str(cfg))

    core.save_config("api_key", "gsk_1")
    text = cfg.read_text(encoding="utf-8")
    assert "# comment" in text
    assert "lang = vi" in text
    assert "api_key = gsk_1" in text
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_core.py -k save_config -v`
Expected: FAIL with `AttributeError: module 'gcm' has no attribute 'save_config'`.

- [ ] **Step 3: Write minimal implementation**

In `gcm`, immediately after `load_config()` ends (after line 92, before `def cfg_bool`):

```python
def save_config(key, value):
    """Ghi 'key = value' vao CONFIG_PATH; cap nhat tai cho neu da co.

    Giu nguyen comment va cac key khac. Tao thu muc/file neu chua co.
    """
    os.makedirs(os.path.dirname(CONFIG_PATH), exist_ok=True)
    lines = []
    if os.path.exists(CONFIG_PATH):
        with open(CONFIG_PATH, encoding="utf-8", errors="replace") as f:
            lines = f.read().splitlines()
    out, replaced = [], False
    for line in lines:
        stripped = line.strip()
        if stripped and not stripped.startswith("#") and "=" in stripped:
            k = stripped.partition("=")[0].strip().lower()
            if k == key.lower():
                out.append(f"{key} = {value}")
                replaced = True
                continue
        out.append(line)
    if not replaced:
        out.append(f"{key} = {value}")
    with open(CONFIG_PATH, "w", encoding="utf-8") as f:
        f.write("\n".join(out) + "\n")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_core.py -k save_config -v`
Expected: PASS (2 passed).

- [ ] **Step 5: Commit**

```bash
git add gcm tests/test_core.py
git commit -m "feat(gcm): add save_config to persist a config key in place

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: `last_repo` config key + `set_repo(folder)` (non-exiting repo switch)

**Files:**
- Modify: `gcm` line 61 (`CONFIG_KEYS`) and add `set_repo` near `ensure_repo` (around line 199)
- Test: `tests/test_core.py`

- [ ] **Step 1: Write the failing test**

Append to `tests/test_core.py`:

```python
def test_set_repo_ok(core, git_repo):
    ok, msg = core.set_repo(str(git_repo))
    assert ok is True
    assert msg == ""
    assert core.REPO_ROOT == str(git_repo)


def test_set_repo_not_a_repo(core, tmp_path):
    plain = tmp_path / "plain"
    plain.mkdir()
    ok, msg = core.set_repo(str(plain))
    assert ok is False
    assert "git" in msg.lower()


def test_last_repo_is_a_config_key(core):
    assert "last_repo" in core.CONFIG_KEYS
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_core.py -k "set_repo or last_repo" -v`
Expected: FAIL (`set_repo` missing; `last_repo` not in CONFIG_KEYS).

- [ ] **Step 3: Write minimal implementation**

In `gcm` line 61, add `last_repo` to `CONFIG_KEYS`:

```python
CONFIG_KEYS = ("api_key", "lang", "model", "tui", "push", "coauthor", "last_repo")
```

Then add after `ensure_repo()` (after line 198):

```python
def set_repo(folder):
    """Chuyen sang thu muc repo cho GUI (khong sys.exit).

    Tra (ok, error_msg). Khi ok, doi cwd va set REPO_ROOT = toplevel.
    """
    global REPO_ROOT
    if not folder or not os.path.isdir(folder):
        return False, "thu muc khong ton tai"
    res = subprocess.run(
        ["git", "rev-parse", "--is-inside-work-tree", "--show-toplevel"],
        cwd=folder, capture_output=True, text=True,
        encoding="utf-8", errors="replace",
    )
    lines = res.stdout.splitlines()
    if res.returncode != 0 or not lines or lines[0].strip() != "true":
        return False, "khong phai git repository"
    top = lines[1].strip() if len(lines) > 1 else folder
    os.chdir(top)
    REPO_ROOT = top
    return True, ""
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_core.py -k "set_repo or last_repo" -v`
Expected: PASS (3 passed).

- [ ] **Step 5: Commit**

```bash
git add gcm tests/test_core.py
git commit -m "feat(gcm): add set_repo and last_repo config key for GUI

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: `stage_files(selected, all_changed)` — sync git index to checkboxes

**Files:**
- Modify: `gcm` (add near `changed_files`, around line 214)
- Test: `tests/test_core.py`

- [ ] **Step 1: Write the failing test**

Append to `tests/test_core.py`:

```python
import subprocess


def _write(repo, name, text):
    (repo / name).write_text(text, encoding="utf-8")


def test_stage_files_adds_selected_and_resets_rest(core, git_repo):
    _write(git_repo, "a.txt", "aaa")
    _write(git_repo, "b.txt", "bbb")
    all_changed = [p for _, p in core.changed_files()]
    assert set(all_changed) == {"a.txt", "b.txt"}

    core.stage_files(["a.txt"], all_changed)

    staged = subprocess.run(
        ["git", "diff", "--cached", "--name-only"],
        cwd=git_repo, capture_output=True, text=True,
    ).stdout.split()
    assert staged == ["a.txt"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_core.py -k stage_files -v`
Expected: FAIL (`stage_files` missing).

- [ ] **Step 3: Write minimal implementation**

In `gcm`, after `changed_files()` (after line 214):

```python
def stage_files(selected, all_changed):
    """Stage cac file trong `selected`, unstage phan con lai trong `all_changed`.

    Dam bao `git diff --staged` chi chua dung cac file nguoi dung tick.
    """
    selected = set(selected)
    to_add = [p for p in all_changed if p in selected]
    to_reset = [p for p in all_changed if p not in selected]
    if to_add:
        run_git(["add", "--", *to_add])
    if to_reset:
        run_git(["reset", "-q", "--", *to_reset], check=False)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_core.py -k stage_files -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add gcm tests/test_core.py
git commit -m "feat(gcm): add stage_files to sync index with GUI selection

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: `generate_message(...)` + `push_simple()` — non-interactive cores for GUI

**Files:**
- Modify: `gcm` (add `generate_message` near `build_prompt`/`call_groq` ~line 564; `push_simple` near `do_push` ~line 686)
- Test: `tests/test_core.py`

- [ ] **Step 1: Write the failing test**

Append to `tests/test_core.py`:

```python
def test_generate_message_uses_staged_diff(core, git_repo, monkeypatch):
    _write(git_repo, "a.txt", "hello world")
    core.stage_files(["a.txt"], ["a.txt"])

    captured = {}

    def fake_call_groq(api_key, system, user, model=None, temperature=0.3):
        captured["user"] = user
        captured["api_key"] = api_key
        return "feat: add a.txt"

    monkeypatch.setattr(core, "call_groq", fake_call_groq)

    msg = core.generate_message(["a.txt"], vietnamese=False, hint="add file",
                                api_key="gsk_test", model="m")
    assert msg == "feat: add a.txt"
    assert "hello world" in captured["user"]  # real staged diff fed to prompt
    assert "add file" in captured["user"]      # hint included
    assert captured["api_key"] == "gsk_test"


def test_generate_message_empty_selection_raises(core, git_repo):
    import pytest
    with pytest.raises(ValueError):
        core.generate_message([], vietnamese=False, hint=None,
                              api_key="k", model="m")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_core.py -k generate_message -v`
Expected: FAIL (`generate_message` missing).

- [ ] **Step 3: Write minimal implementation**

In `gcm`, after `build_prompt`/`call_groq` (after line 619, before `def add_coauthor`):

```python
def generate_message(files, vietnamese, hint, api_key, model):
    """Tao commit message tu cac file DA stage (khong UI, khong sys.exit).

    Goi smart_diff -> build_prompt -> call_groq. Raise ValueError neu thieu file.
    """
    if not files:
        raise ValueError("chua chon file nao")
    diff, skipped, _truncated = smart_diff(files)
    system, user = build_prompt(diff, files, vietnamese, hint, skipped)
    return call_groq(api_key, system, user, model)
```

And after `do_push()` (after line 685, before `def take_value`):

```python
def push_simple():
    """Push cho GUI: tu set upstream neu thieu, khong hoi input. Tra (ok, text)."""
    res = subprocess.run(
        ["git", "push"], cwd=REPO_ROOT,
        capture_output=True, text=True, encoding="utf-8", errors="replace",
    )
    out = (res.stdout + res.stderr).strip()
    if res.returncode == 0:
        return True, out or "push xong."
    if "--set-upstream" in out or "no upstream" in out:
        branch, _ = branch_status()
        res = subprocess.run(
            ["git", "push", "-u", "origin", branch], cwd=REPO_ROOT,
            capture_output=True, text=True, encoding="utf-8", errors="replace",
        )
        out2 = (res.stdout + res.stderr).strip()
        return res.returncode == 0, out2
    return False, out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_core.py -k generate_message -v`
Expected: PASS (2 passed). Then run full suite: `python -m pytest tests/ -v` → all PASS.

- [ ] **Step 5: Commit**

```bash
git add gcm tests/test_core.py
git commit -m "feat(gcm): add generate_message and push_simple cores for GUI

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: `is_double_clicked()` + `--gui` flag detection

**Files:**
- Modify: `gcm` (add near `enable_windows_ansi`, around line 146)
- Test: `tests/test_core.py`

- [ ] **Step 1: Write the failing test**

Append to `tests/test_core.py`:

```python
def test_is_double_clicked_false_on_non_windows(core, monkeypatch):
    monkeypatch.setattr(core.os, "name", "posix")
    assert core.is_double_clicked() is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_core.py -k double_clicked -v`
Expected: FAIL (`is_double_clicked` missing).

- [ ] **Step 3: Write minimal implementation**

In `gcm`, after `enable_windows_ansi()` (after line 145):

```python
def is_double_clicked():
    """True neu exe duoc mo bang double-click trong Explorer (Windows).

    Explorer tao 1 console moi chi co dung process cua ta gan vao. Khi chay
    trong terminal san co thi co >1 process. Non-Windows luon tra False.
    """
    if os.name != "nt":
        return False
    try:
        import ctypes
        arr = (ctypes.c_uint32 * 2)()
        count = ctypes.windll.kernel32.GetConsoleProcessList(arr, 2)
        return count <= 1
    except Exception:
        return False
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_core.py -k double_clicked -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add gcm tests/test_core.py
git commit -m "feat(gcm): detect double-click launch via GetConsoleProcessList

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: `gcm_gui.py` — the Tkinter window (manual test)

**Files:**
- Create: `gcm_gui.py`

This task is GUI code; verification is manual (Task 9). Tkinter is hard to unit-test, so no automated test here.

- [ ] **Step 1: Create `gcm_gui.py` (full file)**

```python
"""gcm GUI - cua so Tkinter (layout 2 cot) cho gcm.

Nhan `core` (module gcm) qua run_gui(core); KHONG import gcm truc tiep
(file `gcm` khong co duoi .py). Goi core.changed_files(), core.set_repo(), ...
"""

import os
import threading
import tkinter as tk
from tkinter import filedialog, messagebox, ttk


def run_gui(core):
    app = GcmApp(core)
    app.mainloop()


def _api_key(core):
    """Lay api_key tu env/config; '' neu chua co (khong sys.exit nhu get_api_key)."""
    return (os.environ.get("GROQ_API_KEY", "").strip()
            or core.CONFIG.get("api_key", ""))


class GcmApp(tk.Tk):
    def __init__(self, core):
        super().__init__()
        self.core = core
        self.title(f"gcm v{core.VERSION}")
        self.geometry("820x520")
        self.minsize(680, 420)
        self.file_vars = []      # list of (tk.BooleanVar, path)

        self._build_repo_bar()
        self._build_body()
        self._build_actions()

        # Mo san repo gan nhat neu con ton tai
        last = core.CONFIG.get("last_repo", "")
        if last and os.path.isdir(last):
            self._open_repo(last)

    # ---- layout ----
    def _build_repo_bar(self):
        bar = ttk.Frame(self, padding=8)
        bar.pack(fill="x")
        ttk.Label(bar, text="📁 Repo:").pack(side="left")
        self.repo_var = tk.StringVar(value="(chua chon)")
        ttk.Label(bar, textvariable=self.repo_var, foreground="#555").pack(
            side="left", padx=6)
        ttk.Button(bar, text="Chọn…", command=self._choose_repo).pack(
            side="right")

    def _build_body(self):
        body = ttk.Panedwindow(self, orient="horizontal")
        body.pack(fill="both", expand=True, padx=8)

        left = ttk.Labelframe(body, text="File thay đổi", padding=6)
        self.files_frame = ttk.Frame(left)
        self.files_frame.pack(fill="both", expand=True)
        body.add(left, weight=1)

        right = ttk.Labelframe(body, text="Commit message (sửa được)", padding=6)
        self.msg_text = tk.Text(right, wrap="word", height=10)
        self.msg_text.pack(fill="both", expand=True)
        body.add(right, weight=2)

    def _build_actions(self):
        row = ttk.Frame(self, padding=8)
        row.pack(fill="x")
        ttk.Label(row, text="Gợi ý (-m):").pack(side="left")
        self.hint_var = tk.StringVar()
        ttk.Entry(row, textvariable=self.hint_var, width=24).pack(
            side="left", padx=4)

        default_lang = "vi" if self.core.CONFIG.get("lang", "").lower() == "vi" \
            else "en"
        self.lang_var = tk.StringVar(value=default_lang)
        ttk.Radiobutton(row, text="vi", variable=self.lang_var,
                        value="vi").pack(side="left")
        ttk.Radiobutton(row, text="en", variable=self.lang_var,
                        value="en").pack(side="left")

        self.push_var = tk.BooleanVar(value=False)
        ttk.Checkbutton(row, text="Push", variable=self.push_var).pack(
            side="right")
        self.commit_btn = ttk.Button(row, text="✓ Commit",
                                     command=self._commit)
        self.commit_btn.pack(side="right", padx=4)
        self.gen_btn = ttk.Button(row, text="⚡ Sinh message",
                                  command=self._generate)
        self.gen_btn.pack(side="right", padx=4)

        self.status_var = tk.StringVar(value="sẵn sàng")
        self.status = ttk.Label(self, textvariable=self.status_var,
                                anchor="w", padding=(8, 4))
        self.status.pack(fill="x")

    # ---- helpers ----
    def _set_status(self, text, color="#555"):
        self.status_var.set(text)
        self.status.configure(foreground=color)

    def _choose_repo(self):
        folder = filedialog.askdirectory(title="Chọn thư mục repo git")
        if folder:
            self._open_repo(folder)

    def _open_repo(self, folder):
        ok, msg = self.core.set_repo(folder)
        if not ok:
            self._set_status(f"Lỗi: {msg}", "#c00")
            return
        self.repo_var.set(self.core.REPO_ROOT)
        self.core.save_config("last_repo", self.core.REPO_ROOT)
        self._reload_files()
        self._set_status("đã mở repo", "#080")

    def _reload_files(self):
        for w in self.files_frame.winfo_children():
            w.destroy()
        self.file_vars = []
        for code, path in self.core.changed_files():
            var = tk.BooleanVar(value=True)
            label = f"{code.strip() or '?':<2} {path}"
            ttk.Checkbutton(self.files_frame, text=label,
                            variable=var).pack(anchor="w")
            self.file_vars.append((var, path))

    def _selected(self):
        return [p for var, p in self.file_vars if var.get()]

    def _all_paths(self):
        return [p for _var, p in self.file_vars]

    # ---- actions ----
    def _generate(self):
        api_key = _api_key(self.core)
        if not api_key:
            api_key = self._ask_api_key()
            if not api_key:
                return
        selected = self._selected()
        if not selected:
            self._set_status("Hãy chọn ít nhất 1 file.", "#c00")
            return

        self.gen_btn.configure(state="disabled")
        self._set_status("Đang sinh message…")
        vietnamese = self.lang_var.get() == "vi"
        hint = self.hint_var.get().strip() or None
        model = self.core.MODEL

        def work():
            try:
                self.core.stage_files(selected, self._all_paths())
                msg = self.core.generate_message(
                    selected, vietnamese, hint, api_key, model)
                self.after(0, lambda: self._on_generated(msg))
            except Exception as e:  # noqa: BLE001 - hien thi moi loi cho user
                self.after(0, lambda: self._on_error(str(e)))

        threading.Thread(target=work, daemon=True).start()

    def _on_generated(self, msg):
        self.msg_text.delete("1.0", "end")
        self.msg_text.insert("1.0", msg)
        self.gen_btn.configure(state="normal")
        self._set_status("đã sinh message — kiểm tra rồi Commit", "#080")

    def _on_error(self, text):
        self.gen_btn.configure(state="normal")
        self._set_status(f"Lỗi: {text}", "#c00")

    def _commit(self):
        message = self.msg_text.get("1.0", "end").strip()
        if not message:
            self._set_status("Message trống.", "#c00")
            return
        message = self.core.add_coauthor(message) if self.push_var.get() \
            else message
        rc = self.core.commit_with_message(message)
        if rc != 0:
            self._set_status("commit thất bại (xem terminal/git).", "#c00")
            return
        if self.push_var.get():
            ok, out = self.core.push_simple()
            if not ok:
                self._set_status(f"đã commit, push lỗi: {out}", "#c00")
                self._reload_files()
                return
        self._set_status("✓ commit xong" + (" + push" if self.push_var.get()
                                             else ""), "#080")
        self.msg_text.delete("1.0", "end")
        self._reload_files()

    def _ask_api_key(self):
        from tkinter import simpledialog
        key = simpledialog.askstring(
            "Groq API key",
            "Dán Groq API key (lấy free tại console.groq.com/keys):",
            parent=self)
        if key:
            key = key.strip()
            self.core.save_config("api_key", key)
            self.core.CONFIG["api_key"] = key
        return key
```

- [ ] **Step 2: Syntax check (no Tk display needed)**

Run: `python -c "import ast; ast.parse(open('gcm_gui.py').read()); print('ok')"`
Expected: `ok`. (Full GUI run is verified in Task 9 on Windows.)

- [ ] **Step 3: Commit**

```bash
git add gcm_gui.py
git commit -m "feat(gui): add Tkinter two-column window for gcm

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Wire `main()` to launch the GUI

**Files:**
- Modify: `gcm` `main()` (around line 752, right after `argv = sys.argv[1:]`)

- [ ] **Step 1: Add the launch branch**

In `gcm`, inside `main()`, locate:

```python
    argv = sys.argv[1:]
    if "-h" in argv or "--help" in argv:
```

Insert between those two lines so it reads:

```python
    argv = sys.argv[1:]
    if argv == ["--gui"] or (not argv and is_double_clicked()):
        # An cua so console den khi double-click (chi Windows; an toan voi
        # subprocess git con vi chung tu tao console rieng neu can).
        if os.name == "nt":
            try:
                import ctypes
                ctypes.windll.kernel32.FreeConsole()
            except Exception:
                pass
        import gcm_gui
        gcm_gui.run_gui(sys.modules[__name__])
        return
    if "-h" in argv or "--help" in argv:
```

- [ ] **Step 2: Verify CLI still behaves (regression)**

Run: `cd /home/devduide/devduide_project/tools/gcm && python gcm -v`
Expected: prints `gcm v2.1.1` (GUI branch NOT taken — args present, not double-click).

Run: `python gcm -h`
Expected: prints logo + help (unchanged).

- [ ] **Step 3: Verify full test suite still green**

Run: `python -m pytest tests/ -v`
Expected: all PASS.

- [ ] **Step 4: Commit**

```bash
git add gcm
git commit -m "feat(gcm): launch GUI on double-click or --gui flag

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: Build packaging + manual GUI verification (Windows)

**Files:**
- Modify: `packaging/windows/build.ps1` (build step around line 44)

- [ ] **Step 1: Bundle `gcm_gui.py` into the exe**

In `build.ps1`, replace the build step (lines 43-44):

```powershell
Copy-Item gcm gcm_entry.py -Force
& $py -m PyInstaller --onefile --name gcm gcm_entry.py
```

with:

```powershell
Copy-Item gcm gcm_entry.py -Force
# gcm_gui.py da nam o thu muc goc repo; --paths . de PyInstaller tim thay no.
& $py -m PyInstaller --onefile --name gcm `
    --hidden-import gcm_gui `
    --paths . `
    gcm_entry.py
```

Also extend the cleanup line (line 78) to keep nothing extra — `gcm_gui.py` is a source file, do NOT delete it; only `gcm_entry.py` is generated. Leave line 78 unchanged (it already only removes `gcm_entry.py`, `build`, `dist`, `gcm.spec`).

- [ ] **Step 2: Build on Windows**

Run (on a Windows machine, in repo root):
`powershell -ExecutionPolicy Bypass -File packaging\windows\build.ps1`
Expected: smoke test prints `gcm v2.1.1`, produces `gcm-2.1.1.msi`.

- [ ] **Step 3: Manual GUI verification checklist**

Install the MSI, then verify each:

- [ ] Double-click `gcm.exe` (Explorer) → GUI mở, **không** còn cửa sổ console đen.
- [ ] Mở terminal, gõ `gcm -v` → in version (CLI vẫn chạy, không mở GUI).
- [ ] Mở terminal, gõ `gcm --gui` → GUI mở (đường thoát thủ công).
- [ ] GUI: bấm **Chọn…**, chọn một repo git → danh sách file hiện ra, có checkbox.
- [ ] Chọn folder KHÔNG phải git → thanh trạng thái đỏ, không crash.
- [ ] Lần đầu chưa có API key → bấm **Sinh message** hiện hộp nhập key; nhập xong lưu lại, lần sau không hỏi nữa.
- [ ] Tick vài file + bấm **Sinh message** → trạng thái "Đang sinh…", cửa sổ không treo, message hiện ở ô phải.
- [ ] Sửa message → bấm **Commit** (không tick Push) → "✓ commit xong", danh sách file làm mới.
- [ ] Tick **Push** + **Commit** trên repo có remote → commit + push, có Co-authored-by.
- [ ] Đóng và mở lại GUI → repo lần trước (`last_repo`) tự mở sẵn.

- [ ] **Step 4: Commit**

```bash
git add packaging/windows/build.ps1
git commit -m "build(windows): bundle gcm_gui.py into the onefile exe

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Notes / Known constraints

- **Dev máy Linux:** Task 1-8 (logic + test) chạy được trên Linux. Task 9 (build + GUI) cần Windows. Nếu Linux có `python3-tk`, có thể chạy thử `python gcm --gui` để xem layout, nhưng luồng double-click + FreeConsole chỉ kiểm chứng trên Windows.
- **FreeConsole:** exe vẫn build dạng console (không `--windowed`) để CLI hoạt động; nhánh GUI trong `main()` (Task 8) gọi `FreeConsole()` để ẩn cửa sổ console đen khi double-click. Cần xác nhận khi kiểm thủ công (Task 9) rằng git subprocess vẫn chạy bình thường sau `FreeConsole` — nếu có vấn đề, phương án dự phòng là dựng `Tk` root trước rồi mới `FreeConsole`.
- **YAGNI:** không làm diff-viewer, model picker, amend, coauthor settings, self-update, Start Menu shortcut, right-click Explorer trong MVP (xem spec mục 9).
